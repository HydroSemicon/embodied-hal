name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Python checks
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-dev.txt

      - name: Install check dependencies
        run: python -m pip install --requirement requirements-dev.txt

      - name: Lint
        run: ruff check --output-format=github kokomi_raspi.py tests

      - name: Check Python syntax
        run: python -m compileall -q kokomi_raspi.py tests

      - name: Test HTTP and command contracts
        run: pytest
